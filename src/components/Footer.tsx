const Footer = () => {
  return (
    <footer className="fixed bottom-0 left-0 right-0 z-40 py-4 pointer-events-none">
      <div className="max-w-7xl mx-auto px-4">
        <div className="text-center text-white/40 text-sm">
          Desenvolvido com muito Control + C e Control + V usando React, TypeScript e muita IA
        </div>
      </div>
    </footer>
  );
};

export default Footer;

